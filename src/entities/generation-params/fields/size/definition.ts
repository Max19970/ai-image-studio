import type { GenerationParamFieldDefinition } from '../../types';
import { SizeField } from './SizeField';
export default { id: 'generationParams.size', Component: SizeField } satisfies GenerationParamFieldDefinition;
