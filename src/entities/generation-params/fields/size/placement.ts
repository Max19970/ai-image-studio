import type { GenerationParamFieldPlacement } from '../../types';

export const placement: GenerationParamFieldPlacement = {
  id: 'composer.params.frame.size',
  slot: 'composer/parameters/frame',
  use: 'generationParams.size',
  order: 10
};
