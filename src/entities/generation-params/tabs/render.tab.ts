import type { GenerationParamTabDefinition } from '../types';

export const renderTab = { id: 'render', slot: 'composer/parameters/render', labelKey: 'params.render', hintKey: 'params.renderHint' } satisfies GenerationParamTabDefinition;
