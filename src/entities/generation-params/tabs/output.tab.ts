import type { GenerationParamTabDefinition } from '../types';

export const outputTab = { id: 'output', slot: 'composer/parameters/output', labelKey: 'params.formatStreaming', hintKey: 'params.outputHint' } satisfies GenerationParamTabDefinition;
