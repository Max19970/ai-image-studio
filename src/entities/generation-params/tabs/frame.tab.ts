import type { GenerationParamTabDefinition } from '../types';

export const frameTab = { id: 'frame', slot: 'composer/parameters/frame', labelKey: 'params.frame', hintKey: 'params.frameHint' } satisfies GenerationParamTabDefinition;
