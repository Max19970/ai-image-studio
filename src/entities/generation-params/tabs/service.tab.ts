import type { GenerationParamTabDefinition } from '../types';

export const serviceTab = { id: 'service', slot: 'composer/parameters/service', labelKey: 'params.service', hintKey: 'params.serviceHint' } satisfies GenerationParamTabDefinition;
